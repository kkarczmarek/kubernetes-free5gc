apiVersion: v1
kind: Pod
metadata:
  name: t-deny-limits-lt-requests
  namespace: playground
spec:
  containers:
    - name: c
      image: docker.io/library/busybox:1.36.1
      command: ["sh","-c","sleep 5"]
      resources:
        requests: {cpu: "200m", memory: "256Mi"}
        limits:   {cpu: "100m", memory: "128Mi"}
