apiVersion: v1
kind: Pod
metadata:
  name: mutate-defaults-demo
  labels:
    app: mutate-defaults-demo
    app.kubernetes.io/part-of: free5gc
    project: free5gc
spec:
  containers:
  - name: main
    image: docker.io/library/busybox:1.36
    command: ["sh","-c","sleep 3600"]
