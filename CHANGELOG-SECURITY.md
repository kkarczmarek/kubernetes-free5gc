# Changelog — security hardening
- Added `policies/` (PSA labels, default-deny, allow-sig-plane, Kyverno NET_ADMIN gate, verify-images skeleton)
- Added `rbac/` (ops-patcher, readonly-ops, rolebinding example)
- Added `docs/SECCONTROL_SUMMARY.md`
- Extended `.gitignore` to avoid accidental secret leakage
